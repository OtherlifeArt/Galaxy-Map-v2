# version: '3.9' Obsolete

networks:
  frontend:
    name: ${APP_NAME}-frontend

services:
  web-dev:
    image: ${DEV_IMAGE_NAME}
    container_name: ${APP_NAME}-web-dev
    restart: ${RESTART_POLICY}
    ports:
      - '${DEV_HOST_PORT}:80'
    volumes:
      - ./:/usr/local/apache2/htdocs
    networks:
      - frontend